version: "2.3"
name: "design-planning-quality-dashboard"
repo:
  url: "https://github.com/innuama-coder/spec-executor-testing-09-planning.git"
  branch: "main"
task_dir: "tasks/design-planning"
executor: "codex"
execution:
  poll_interval_sec: 2
  idle_timeout_sec: 90
  max_duration_min: 15
  max_nudge_count: 3
  max_consecutive_escalate: 2
  env:
    file: "ENV"
    required: false
    privacy: "redacted"
  agent:
    id: "design-planning-agent"
    name: "design-planning-agent"
    role: "生成设计任务规划，保证 DAG、并行执行、停止条件和验收标准完整。"
    model:
      preference: "default"
    instructions:
      - "monitoring side-effect guard、Codex planning Agent、review。"
      - "严格遵守任务包中的 PROMPT、AGENTS、CLAUDE 与 docs 上下文。"
      - "ENV 仅用于进程环境注入，不得作为上下文文件读取或在默认输出中泄漏变量值。"
      - "若存在 review items，必须在同一 JOB 内完成修复、验证和交付说明更新。"
    context:
      files:
        - "PROMPT.md"
        - "AGENTS.md"
        - "CLAUDE.md"
      exclude:
        - "ENV"
    tools:
      allow:
        - "shell"
        - "apply_patch"
      deny:
        - "network"
    permissions:
      filesystem: "workspace-write"
      network: "deny"
      approval: "on-request"
  review:
    max_iterations: 2
    items:
      - id: "REV-PLAN-001"
        source: "external-review"
        severity: "must"
        target: "plans/design-plan.md"
        description: "规划必须明确依赖、并行与停止条件。"
        expected_fix: "补齐 DAG、并行批次、验收标准和停止条件。"
        evidence_refs:
          - "PROMPT.md"
        acceptance:
          command: "grep -q \"依赖\" plans/design-plan.md && grep -q \"并行\" plans/design-plan.md && grep -q \"停止\" plans/design-plan.md"
  lifecycle:
    resume:
      require_same_job: true
    monitoring:
      busy_side_effect: "observe_only"
      source_degraded_policy: "bounded_failure"
      screen_capture_semantic_fallback: false
    capture:
      default_raw_output: false
      failure_evidence: true
verification:
  pass_threshold: 1
  commands:
    - "test -s plans/design-plan.md"
    - "grep -q \"依赖\" plans/design-plan.md"
    - "grep -q \"验收\" plans/design-plan.md"
    - "grep -q \"并行\" plans/design-plan.md"
    - "grep -q \"停止\" plans/design-plan.md"
reporting:
  format: "markdown"
  include_evidence_refs: true
  redact_env_values: true
  include_handoff: true
deliverables:
  - path: "plans/design-plan.md"
    verify: "test -s plans/design-plan.md && grep -q \"依赖\" plans/design-plan.md && grep -q \"验收\" plans/design-plan.md && grep -q \"并行\" plans/design-plan.md && grep -q \"停止\" plans/design-plan.md"
